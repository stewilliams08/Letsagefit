# ── Per-client secrets (set these in Railway, NOT in the repo) ──
# GoHighLevel Private Integration token for THIS client's sub-account
GHL_TOKEN=
# GoHighLevel Location (sub-account) ID for this client
GHL_LOCATION_ID=
# Where the durable lead log is written (point at a Railway Volume in prod, e.g. /data/leads.json)
CLICK_LOG_PATH=/tmp/leads.json
# Optional: token required to view /leads (leave blank to disable the viewer)
LOG_VIEW_TOKEN=
# Port (Railway sets this automatically)
PORT=3000
